name: Выкладка на хостинг

# После каждой отправки в main файлы сайта уезжают на Timeweb по FTP.
# Реквизиты лежат в секретах репозитория: FTP_SERVER, FTP_USERNAME, FTP_PASSWORD.
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Загрузить файлы по FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # Timeweb не поддерживает FTP с шифрованием, поэтому обычный FTP.
          # Поэтому же для выкладки заведён отдельный FTP-пользователь, а не главный аккаунт.
          protocol: ftp
          # FTP-пользователь попадает в корень аккаунта, а сайт лежит внутри public_html
          server-dir: ./public_html/
          # Рабочие материалы и служебные файлы на хостинг не нужны
          exclude: |
            **/.git*
            **/.git*/**
            .nojekyll
            assets/**
            *.odt
            Ссылки на фото, видео и отзывы.txt
